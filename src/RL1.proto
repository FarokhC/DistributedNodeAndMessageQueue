//to recompile this proto run
//python -m grpc_tools.protoc -I./protos --python_out=. --grpc_python_out=. RL1.proto

syntax = "proto3";

service MessagePassing {
    rpc GetServerResponse(Message) returns (MessageResponse) {}
}

message Message{
  string testMessage = 1;
  //client will send a num, and server will increment num and reply it back with MessageResponse
  //int32 count = 1;
}

message MessageResponse{
  string testResponse = 2;
  //int32 count = 1;
}
